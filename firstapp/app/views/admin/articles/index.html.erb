  <h1> ARTIKEL </h1>
  <table>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
 
  <% @article.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.body %></td>
      <td><%= link_to 'Show', admin_article_path(article.id) %></td>
      <td><%= link_to 'Edit', edit_admin_article_path(article.id) %></td>
      <td><%= link_to "Delete", admin_article_path(article.id), :confirm => "Are you sure you want to delete this item ?", :method => :delete %></td>
    </tr>
  <% end %>
  </table>
  <br />
  <%= link_to 'New', new_admin_article_path %>
