  <h1> ARTIKEL </h1>
  <table>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.body %></td>
      <td><%= link_to 'Show', article %></td>
      <td><%= link_to 'Edit', edit_article(article) %></td>
      <td><%= link_to "Delete", {:action => 'delete', :id => article.id}, :confirm => "Are you sure you want to delete this item?" %></td>
    </tr>
  <% end %>
  </table>
  <br />
  <%= link_to 'New', new_article_path %>
